import React from 'react'

const FunctionsMenu = ({ editor }) => {
  if (!editor) {
    return null
  }

  return (
    <nav>
      {/* TODO: Buttons */}
    </nav>
  )
}

export default FunctionsMenu
