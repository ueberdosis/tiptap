---
"@tiptap/core": minor
---

Add `crossEditorDrop` editor option to control whether dragging content between editors moves or copies it. Set to `'copy'` to keep the source content when dropping into another editor.
