---
'@tiptap/extension-invisible-characters': minor
---

Added `TabCharacter` and `NonBreakingSpaceCharacter` builders to visualize tab and non-breaking space characters. The feature is opt-in, enable it by passing them to the `builders` option.
