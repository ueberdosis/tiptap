---
'@tiptap/core': minor
---

Add optional `createCustomHandle` callback to `ResizableNodeView` for custom resize handle rendering. When provided, allows full control over handle markup, styling, and positioning.